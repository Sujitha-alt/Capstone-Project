# src/main/resources/application.properties

spring.application.name=Bus-Ticket-Reservation

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/bus1?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sujitha@27
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hikari (optional, good defaults)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# Disable H2 entirely
spring.h2.console.enabled=false
# --- JDBC etc above ---

# JWT (HS256)
app.jwt.secret=${JWT_SECRET:change-me-to-a-very-long-random-string}
app.jwt.expiration-ms=86400000          # 24 hours
app.jwt.issuer=NeoBus
app.jwt.header=Authorization
app.jwt.prefix=Bearer 
